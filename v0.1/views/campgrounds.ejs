<% include partials/header.ejs %>

    <h1> Campgrounds page</h1>

    <!-- here u can find list of campgrounds -->

    <% campgrounds.forEach(function(campground){ %>
        <div>
            <h3> <%= campground.name %></h3>


            <img src="<%= campground.image%>" >
        </div>
        
        <%  }); %>
      
        


<% include partials/footer.ejs %>